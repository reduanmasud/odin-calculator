const CALCULATE = {
    sum: (a, b) => a + b,
    sub: (a, b) => a - b,
    prod: (a, b) => a * b,
    div: (a, b) => (b == 0 ? "ERROE" : a / b),
};

let operand1;
let operand2;
let operator;
let result;


